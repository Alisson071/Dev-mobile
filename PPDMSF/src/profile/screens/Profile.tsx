import {Text, View} from 'react-native';
import { ProductC } from '../../modules/Home';
const Profile = () => {
    return(
        <View style = {{flex:1}}>
           <ProductC></ProductC>
        </View>
    );
};

export default Profile;